extends ../config/template
append config
  -title = "EDgrid | Core (CSS/Sass)"
  -description = "El componente core es el que genera los ed-container y ed-item y sus dimensiones. Uso con CSS (cssCore) Para la version CSS, basta con seguir los pasos para los ed-container y ed-item indicados en el flujo de trabajo con ed-grid Sass Si se trabaja con Sass, es recomendable tener la variable $cssHelpers en true para que se compilen los tamaños para los ed-item y seguir el flujo de trabajo recomendado con ed-grid"
  -ogUrl = "/core.html"

block main
  .ed-container.main-section
    include ../includes/docs-menu
    main(itemscope itemprop="mainContentOfPage" role="main").ed-item.l-75.main-main
      include:markdown-it(html) ../../md/docs/core.md
